{
  "name": "prettier-plugin-tailwindcss",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --external:prettier --outdir=dist",
    "dev": "npm run build -- --watch",
    "test": "node test.mjs"
  },
  "dependencies": {
    "import-fresh": "^3.3.0",
    "import-from": "^4.0.0",
    "recast": "^0.20.5",
    "tailwindcss": "^3.0.12"
  },
  "devDependencies": {
    "esbuild": "^0.14.11",
    "prettier": "^2.5.1",
    "uvu": "^0.5.1"
  },
  "peerDependencies": {
    "prettier": ">=2"
  }
}
